# Identidad con la que este servidor MCP publica en la red (NIP-19). Vacío genera
# una y la guarda en estado/clave.txt. Es la identidad del agente que usa la
# herramienta, no la tuya.
NOSTR_NSEC=
RELAYS=wss://nos.lol,wss://relay.damus.io
# Prueba de trabajo que mina en los pedidos y en las respuestas (bits, NIP-13).
POW_MINIMO=20
POW_RESPUESTA=16
RUTA_CLAVE=estado/clave.txt
